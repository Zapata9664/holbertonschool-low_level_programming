
sxsax#include<stdio.h>

/**
 * main - Write a program that prints all single digit numbers of base 10
 *
 * Return: (0)
 */

init main(void)
{
int num = 0;
for (num <= '9')
++num;
{
putchar(num);
}
putchar('\n');
return (0);
}
